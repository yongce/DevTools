<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/AppTheme.ListViewItem"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/app_icon"
        android:layout_width="@dimen/app_icon_size"
        android:layout_height="@dimen/app_icon_size"
        android:layout_marginRight="4dp"
        android:layout_marginEnd="4dp"
        android:scaleType="fitCenter"
        android:layout_centerVertical="true" />

    <TextView
        android:id="@+id/app_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/app_icon"
        android:layout_toEndOf="@id/app_icon" />

    <TextView
        android:id="@+id/pkg_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/app_name"
        android:layout_alignLeft="@id/app_name"
        android:layout_alignStart="@id/app_name" />

    <TextView
        android:id="@+id/memory_usage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/pkg_name"
        android:layout_alignLeft="@id/pkg_name"
        android:layout_alignStart="@id/pkg_name" />

    <TextView
        android:id="@+id/processes_holder"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/memory_usage"
        android:layout_alignLeft="@id/memory_usage"
        android:layout_alignStart="@id/memory_usage" />
</RelativeLayout>